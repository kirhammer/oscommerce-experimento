server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: unknown paths resolve to the app shell so client-side
    # routing works on hard reloads.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
